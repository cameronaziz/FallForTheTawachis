<h1><%= @reservation.name %></h1>
<%= link_to 'Edit Reservation', edit_reservation_path(@reservation) %>
<%= link_to 'Delete', @reservation, method: :delete, data: {confirm: 'Are you sure?'}  %>
  <p>Party Size: <%= @reservation.party_size_filter %></p>
  <table class="table table-hover">
    <tr>
      <th></th>
      <th><%= @reservation.invitee.first_name %></th>
    </tr>
    <tr>
      <td>Food Choice</td>
    </tr>
  </table>