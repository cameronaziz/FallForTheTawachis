<%= form_for @ticket do |f| %>
    <div class="form-group">
      <%= f.text_field :title, placeholder: 'Ticket Subject' %>
    </div>
    <div class="form-group">
      <%= f.text_area :description, rows: 6, cols: 87 %>
    </div>
    <%= f.submit 'Submit', class: 'btn btn-success' %>
<% end %>
